import React from 'react';

describe('Repository', () => {
  it('should render a title link in the form: author/repository', () => {
    expect(1).toEqual(2);
  });

  it('header should render repository description', () => {
    expect(1).toEqual(2);
  });

  it('header should render the programming language', () => {
    expect(1).toEqual(2);
  });

  it('header should render the star count', () => {
    expect(1).toEqual(2);
  });

  it('header should render the fork count', () => {
    expect(1).toEqual(2);
  });

  it('should render the contributors', () => {
    expect(1).toEqual(2);
  });

  it('should render a button to mark as starred', () => {
    expect(1).toEqual(2);
  });

  it('should render a button to unmark as starred when it is already starred', () => {
    expect(1).toEqual(2);
  });

  it('should render stars per date range', () => {
    expect(1).toEqual(2);
  });
});
