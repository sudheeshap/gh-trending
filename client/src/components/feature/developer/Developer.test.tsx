import React from 'react';

describe('Developer', () => {
  it('should render the rank', () => {
    expect(1).toEqual(2);
  });

  it('should render a title link with the name', () => {
    expect(1).toEqual(2);
  });

  it('header should render the username', () => {
    expect(1).toEqual(2);
  });

  it('header should render popular repository if available', () => {
    expect(1).toEqual(2);
  });

  it('header should not render popular repository section if not available', () => {
    expect(1).toEqual(2);
  });

  it('header should render the follow button', () => {
    expect(1).toEqual(2);
  });

  it('header should render the unfollow button if already followed', () => {
    expect(1).toEqual(2);
  });
});
